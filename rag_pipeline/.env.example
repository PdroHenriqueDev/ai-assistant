# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: OpenAI Organization ID (if you belong to multiple organizations)
# OPENAI_ORG_ID=org-your-organization-id

# Optional: Custom OpenAI API Base URL (for Azure OpenAI or other providers)
# OPENAI_API_BASE=https://your-custom-endpoint.openai.azure.com/

# Optional: API Version (for Azure OpenAI)
# OPENAI_API_VERSION=2023-12-01-preview

# Pipeline Configuration
# Base URL for scraping (default: https://ajuda.infinitepay.io/pt-BR/)
# INFINITEPAY_BASE_URL=https://ajuda.infinitepay.io/pt-BR/

# Vector store type: faiss or chroma (default: faiss)
# VECTOR_STORE_TYPE=faiss

# Directory for vector store persistence (default: ./infinitepay_vector_store)
# VECTOR_STORE_DIR=./infinitepay_vector_store

# File for storing scraped articles (default: infinitepay_articles.json)
# ARTICLES_FILE=infinitepay_articles.json

# Scraping Configuration
# Maximum number of articles to scrape (default: no limit)
# MAX_ARTICLES=100

# Delay between requests in seconds (default: 0.5)
# SCRAPING_DELAY=0.5

# User agent for web scraping
# USER_AGENT=InfinitePayRAG/1.0 (Educational Purpose)

# Document Processing Configuration
# Chunk size for text splitting (default: 1200)
# CHUNK_SIZE=1200

# Chunk overlap for text splitting (default: 150)
# CHUNK_OVERLAP=150

# Model for token counting (default: gpt-4o-mini)
# TOKEN_COUNT_MODEL=gpt-4o-mini

# RAG Chain Configuration
# LLM model name (default: gpt-4o-mini)
# LLM_MODEL=gpt-4o-mini

# LLM temperature (default: 0)
# LLM_TEMPERATURE=0

# Number of documents to retrieve (default: 5)
# RETRIEVER_K=5

# Search type: similarity or mmr (default: mmr)
# SEARCH_TYPE=mmr

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO

# Log file path (default: logs to console only)
# LOG_FILE=rag_pipeline.log